name: CI
on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v4
      - run: corepack enable
      - uses: actions/cache@v5
        with:
          path: ~/.local/share/pnpm/store
          key: pnpm-${{ hashFiles('pnpm-lock.yaml') }}
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm typecheck
      - run: pnpm --filter react-native-fabric-shimmer test
      - run: pnpm --filter react-native-fabric-shimmer build
      - run: pnpm --filter react-native-fabric-shimmer package:check
      - run: pnpm --filter react-native-fabric-shimmer size

  perf:
    runs-on: ubuntu-latest
    needs: gate
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - uses: jdx/mise-action@v4
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      # Stage the committed baseline where Reassure expects to find it.
      # The committed baseline is updated by maintainers on main when an
      # intentional perf change is merged.
      - name: Stage perf baseline
        run: |
          mkdir -p packages/react-native-fabric-shimmer/.reassure
          cp packages/react-native-fabric-shimmer/__perf__/baseline.json \
             packages/react-native-fabric-shimmer/.reassure/baseline.perf
      - run: pnpm --filter react-native-fabric-shimmer test:perf
      - name: Upload perf comparison report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: reassure-output
          path: |
            packages/react-native-fabric-shimmer/.reassure/output.md
            packages/react-native-fabric-shimmer/.reassure/output.json
            packages/react-native-fabric-shimmer/.reassure/current.perf
          if-no-files-found: ignore
