---
"react-native-dynamic-shimmer": minor
---

Initial release — `<Skeleton>` component with runtime fiber walk, Fabric measurement, shimmer / pulse / none animations, IR helpers (`walk` / `find` / `findAll` / `hide` / `merge` / `union`), React Compiler safe, Reduce Motion respected via `ReduceMotion.System`, 83 tests across 19 files covering pure helpers, hooks, and measurement pipeline.
